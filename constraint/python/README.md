# Uputstvo

Potrebno je imati instaliran Python 3.7 i biblioteku `python-constraint`. Na Ubuntu operativnom sistemu, biblioteka `python-constraint` se može instalirati pomoću Pip alata:

```bash
sudo apt-get -y install python3-pip
sudo pip3 install python-constraint
```

Rešenja su imenovana prema redosledu zadataka u PDF-u, u formatu `2.SECTION.TASK.py`.
Na primer, rešenje zadatka 4 iz sekcije 3 nalazi se u fajlu `2.3.4.py`.

# Korisni linkovi i literatura

- [Python-constraint documentation](http://labix.org/doc/constraint/)
- [Python-constraint na PyPI](https://pypi.python.org/pypi/python-constraint)
- [Constraint programming blog](http://www.hakank.org/constraint_programming_blog/)
